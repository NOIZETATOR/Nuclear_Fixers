using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ONAPLUSLETEMPS : MonoBehaviour
{
    public Player p;
    // Start is called before the first frame update
    void Start()
    {
        p.IsInMenu = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
